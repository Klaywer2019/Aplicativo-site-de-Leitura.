<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sinfonia Rúnica - Estúdio</title>
    <style>
        body { background: #0a0a0a; color: white; font-family: sans-serif; margin: 0; padding: 20px; text-align: center; }
        
        .wave-container {
            height: 100px; background: #1a1a1a; border-radius: 15px; 
            margin-bottom: 20px; display: flex; align-items: center; justify-content: center;
            border: 1px solid #6a1b9a; overflow: hidden;
        }

        .pad-grid {
            display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; margin-top: 20px;
        }

        .music-pad {
            height: 80px; background: #2d3436; border-radius: 12px;
            border: none; color: white; font-weight: bold; cursor: pointer;
            transition: 0.2s; box-shadow: 0 4px #000;
        }

        .music-pad:active { transform: translateY(4px); box-shadow: 0 0 #000; background: #6a1b9a; }

        .controles {
            background: #1e1e1e; padding: 20px; border-radius: 20px; margin-top: 30px;
        }
    </style>
</head>
<body>

    <h2 style="color: #a29bfe;">🎹 Sinfonia Rúnica</h2>
    
    <div class="wave-container">
        <div style="color: #55efc4; font-size: 0.8rem; letter-spacing: 5px;">||||||||||||||||||||||</div>
    </div>

    <div class="controles">
        <button onclick="window.location.href='index.html'" style="background:#444; color:white; border:none; padding:10px; border-radius:10px; margin-bottom:10px;">← Voltar</button>
        <h4>🎚️ Mesa de Batidas</h4>
        <div class="pad-grid">
            <button class="music-pad" onclick="playEffect('Kick')">BUM</button>
            <button class="music-pad" onclick="playEffect('Snare')">CA</button>
            <button class="music-pad" onclick="playEffect('HiHat')">TCHÍ</button>
            <button class="music-pad" style="background: #e17055;">REC 🔴</button>
            <button class="music-pad" style="background: #00b894;">PLAY ▶️</button>
            <button class="music-pad" style="background: #0984e3;">SAVE 💾</button>
        </div>
    </div>

    <script>
        function playEffect(som) {
            console.log("Tocando: " + som);
            // Aqui você vai colocar os arquivos de áudio depois
        }
    </script>
</body>
</html>
